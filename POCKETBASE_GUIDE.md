# ⚡ คู่มือการใช้งาน PocketBase & Multi-Device LAN Sharing สำหรับ AssetFlow

ระบบ **AssetFlow** ได้รับการติดตั้งและผสานเข้ากับ **PocketBase (Embedded Backend)** ทำให้มีทั้ง **ฐานข้อมูลความเร็วสูง, ระบบจัดเก็บรูปภาพ, หน้าจอ Admin UI, และระบบ Real-time ในตัว** พร้อมความสามารถแชร์ใช้งานร่วมกันหลายเครื่องในวง Wi-Fi / LAN เดียวกันได้ทันที

---

## 🚀 วิธีการเปิดใช้งานระบบ

### 1. ใช้งานหลายเครื่องในวง Wi-Fi / LAN (แนะนำ ⭐)
ดับเบิลคลิกไฟล์ **`start-lan.bat`**
- ระบบจะค้นหา IP Address ของเครื่องคอมพิวเตอร์ของคุณในวงแลนโดยอัตโนมัติ
- เริ่มต้นทำงานทั้ง PocketBase และ AssetFlow
- **เครื่องอื่น (คอมพิวเตอร์, โน้ตบุ๊ก, มือถือ) ในวง Wi-Fi เดียวกัน** สามารถเปิดเบราว์เซอร์แล้วพิมพ์ URL ที่ปรากฏบนหน้าจอ (เช่น `http://192.168.1.61:3000`) เพื่อเข้าใช้งานและอัปเดตข้อมูลพร้อมกันได้ทันที!

### 2. ใช้งานเฉพาะเครื่องนี้ (Standalone)
ดับเบิลคลิกไฟล์ **`start.bat`**
- ระบบจะเปิดหน้าเว็บ `http://localhost:3000` ให้อัตโนมัติ

---

## 🖥️ หน้าจอ PocketBase Admin UI (สำหรับผู้ดูแลระบบ)

PocketBase มีหน้าเว็บ Dashboard สำหรับจัดการฐานข้อมูลและรูปภาพในตัว:

- **URL:** `http://localhost:8090/_/`
- **Email:** `admin@assetflow.local`
- **Password:** `AssetFlow@2569!`

### สิ่งที่ทำได้ใน Admin UI:
1. **ดู/แก้ไขรายการครุภัณฑ์ 93 รายการ:** เข้าไปที่ Collection `assets` เพื่อดูตาราง แก้ไข หรือเพิ่มรูปถ่าย
2. **จัดการสถานที่และหมวดหมู่:** เข้าไปที่ Collection `locations` หรือ `asset_categories`
3. **ส่งออกข้อมูล (Export Data):** สามารถ Export ข้อมูลทั้งหมดเป็นไฟล์ JSON / CSV ได้ในคลิกเดียว
4. **จัดการ API Rules & Permissions:** ปรับแต่งสิทธิ์การเข้าถึงข้อมูลของแต่ละตารางได้แบบละเอียด

---

## 💾 การสำรองข้อมูล (Backup & Restore)

ข้อมูลทั้งหมดของระบบ (ฐานข้อมูล ตารางทั้งหมด บัญชีผู้ใช้ และรูปภาพครุภัณฑ์) จะถูกเก็บไว้ในโฟลเดอร์เดียวคือ:

```text
📁 e:\demo\AssetFlow\pb_data\
```

### วิธีสำรองข้อมูล (Backup):
- เพียงแค่คัดลอกโฟลเดอร์ **`pb_data`** ไปเก็บไว้ใน Flash Drive, External HDD หรือ Google Drive

### วิธีนำข้อมูลกลับมาใช้ (Restore):
- นำโฟลเดอร์ **`pb_data`** ที่สำรองไว้ มาวางทับในโฟลเดอร์โปรเจกต์เดิม แล้วรันโปรแกรมตามปกติ ข้อมูลทุกอย่างจะกลับมาสมบูรณ์ 100%

---

## 📁 โครงสร้างไฟล์ในโปรเจกต์

```text
e:\demo\AssetFlow/
├── pocketbase.exe              # ตัว Backend Engine สำเร็จรูป (Go Binary)
├── pb_data/                    # โฟลเดอร์เก็บฐานข้อมูลและรูปภาพทั้งหมด
│   ├── data.db                 # SQLite Database ของ PocketBase
│   └── storage/                # ที่เก็บไฟล์รูปภาพครุภัณฑ์
├── scripts/
│   └── seed-pocketbase.mjs     # สคริปต์นำเข้าข้อมูลจริง 93 รายการ
├── start-lan.bat               # สคริปต์รันระบบแชร์หลายเครื่องในวง Wi-Fi / LAN
├── start.bat                   # สคริปต์รันระบบในเครื่อง
└── app/                        # ซอร์สโค้ดหน้าเว็บ AssetFlow (Next.js)
```
