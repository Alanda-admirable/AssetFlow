# 🛠️ คู่มือแก้ไขปัญหาเครื่องอื่นเข้าใช้งาน start-lan.bat ไม่ได้ (LAN Troubleshooting)

หากคอมพิวเตอร์ โน้ตบุ๊ก หรือมือถือเครื่องอื่นในวง Wi-Fi / LAN ไม่สามารถเปิดเข้าใช้งาน AssetFlow ได้ (เช่น ขึ้น *This site can't be reached* หรือ *Connection timed out*) ให้ตรวจสอบและแก้ไขตาม 4 สาเหตุหลักนี้ครับ:

---

### 1. ปลดล็อก Windows Firewall (สาเหตุอันดับ 1 ⭐)

โดยปกติ Windows Defender Firewall จะบล็อกการเชื่อมต่อจากภายนอกเข้าพอร์ต 3000 และ 8090

**วิธีแก้ไข:**
1. ไปที่โฟลเดอร์ `e:\demo\AssetFlow`
2. คลิกขวาที่ไฟล์ **[`setup-firewall.bat`](file:///e:/demo/AssetFlow/setup-firewall.bat)** แล้วเลือก **"Run as administrator" (เรียกใช้ในฐานะผู้ดูแลระบบ)**
3. จะมีหน้าต่างขึ้นมาถาม ให้กด **Yes (ใช่)** เพื่อให้ระบบเปิดพอร์ต 3000 และ 8090 ให้อัตโนมัติ

---

### 2. เลือก IP Address ให้ตรงกับเครือข่ายที่ใช้งาน

เนื่องจากคอมพิวเตอร์ของคุณอาจมีทั้ง **Wi-Fi** และ **สาย LAN (Ethernet)** ซึ่งอยู่คนละชุด IP:

- **ถ้าเครื่องปลายทาง (มือถือ/โน้ตบุ๊ก) ต่อ Wi-Fi:** 👉 ให้เข้าผ่าน URL ของ Wi-Fi เช่น `http://192.168.80.10:3000`
- **ถ้าเครื่องปลายทางต่อ สาย LAN:** 👉 ให้เข้าผ่าน URL ของ Ethernet เช่น `http://192.168.1.61:3000`

*(ดูรายการ IP ทั้งหมดได้จากหน้าต่าง `start-lan.bat` ที่เปิดขึ้นมา)*

---

### 3. เปลี่ยนประเภทเครือข่ายเป็น "Private Network" ใน Windows

หากเครือข่ายในเครื่องแม่ถูกตั้งเป็น "Public Network" Windows จะปิดกั้นการแชร์ไฟล์และการเชื่อมต่อทั้งหมด

**วิธีแก้ไข:**
1. กดปุ่ม `Windows + I` เพื่อเปิด **Settings (การตั้งค่า)**
2. ไปที่เมนู **Network & Internet (เครือข่ายและอินเทอร์เน็ต)**
3. คลิกเลือก **Wi-Fi** หรือ **Ethernet** ที่กำลังเชื่อมต่ออยู่
4. ในส่วน **Network profile type** ให้เปลี่ยนจาก **Public** เป็น **Private (เครือข่ายส่วนตัว)**

---

### 4. ตรวจสอบระบบ Wi-Fi Router (AP Isolation / Guest Wi-Fi)

- หากเชื่อมต่อ Wi-Fi ที่เป็น **Guest Network (Wi-Fi สำหรับแขก)** หรือ Router มีการเปิดฟังก์ชัน **AP Isolation / Client Isolation** เร้าเตอร์จะบล็อกไม่ให้อุปกรณ์ใน Wi-Fi คุยกันเอง
- **วิธีแก้:** ให้ทุกคนเชื่อมต่อ Wi-Fi หลักของสำนักงาน หรือให้เครื่องแม่เปิด **Mobile Hotspot** ใน Windows แล้วให้เครื่องอื่นมาต่อ Hotspot โดยตรง
